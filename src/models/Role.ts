/**
 * Defines the roles of the user accounts on PFM.
 */

export enum Role {
  /**
   * Standard PFM user or a customer.
   */
  User = "user",

  /**
   * Administrator can manage the entire platform through admin portal.
   */
  Administrator = "administrator",
}
